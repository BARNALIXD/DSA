// Singly linked list node in Java
class Node {

    // Data field
    int data;

    // Next node
    Node next;

    // Constructor to initialize a new node with data
    Node(int new_data) {
        data = new_data;
        next = null;
    }
}
